{
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.26.3"
  },
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network sepolia",
    "start": "npx http-server frontend -c-1 -p 8080",
    "build:frontend": "echo \"No build step configured. Ensure frontend/ contains index.html.\""
  },
  "dependencies": {
    "ethers": "^6.9.0"
  }
}
